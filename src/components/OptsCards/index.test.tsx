import React from 'react'
import { render, fireEvent, screen } from '@testing-library/react'
import '@testing-library/jest-dom'
import OptsCards from './index'

describe("<OptsCards />", () => {
    it("should render the opts cards", () => {

    })
})